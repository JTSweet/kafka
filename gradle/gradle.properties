# Set memory for the Gradle Daemon (if not set by GRADLE_OPTS)
# org.gradle.jvmargs=-Xmx4g -Xms1g

# Set memory for forked Gradle workers (compilers, test runners, etc.)
# This is the key setting to control the failing process's memory.
org.gradle.workers.max=20
org.gradle.jvmargs=-Xmx4g -Xms1g
